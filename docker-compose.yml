services:
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    env_file:
      - path: ./back/.env
    ports:
      - "3001:3001"
    labels:
      com.orga-planning.service: "Nest Backend"
      com.orga-planning.description: "Backend using framework Nest"
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    env_file:
      - path: ./front/.env.local
    ports:
      - "3000:3000"
    labels:
      com.orga-planning.service: "Nest Frontend"
      com.orga-planning.description: "Front using framework Nuxt"